namespace SilkyNvg.OpenGL
{
    public sealed class GraphicsManager
    {

        private readonly uint _flags;

        public GraphicsManager(uint flags)
        {
            _flags = flags;
        }

    }
}
